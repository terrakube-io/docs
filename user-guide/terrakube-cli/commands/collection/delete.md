# `terrakube collection delete`

Deletes a collection.

## Usage

```bash
terrakube collection delete --organization <ORG> --id <COL_ID>
```

## Examples

```bash
terrakube collection delete -o "$ORG_ID" --id "$COL_ID"
```
