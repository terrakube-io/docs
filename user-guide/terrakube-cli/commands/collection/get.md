# `terrakube collection get`

Gets collection details.

## Usage

```bash
terrakube collection get --organization <ORG> --id <COL_ID>
```

## Examples

```bash
terrakube collection get -o "$ORG_ID" --id "$COL_ID" --output json
```
