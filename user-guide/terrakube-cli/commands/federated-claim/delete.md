# `terrakube federated-claim delete`

Deletes a federated claim rule.

## Usage

```bash
terrakube federated-claim delete --federated <FED> --id <CLAIM_ID>
```

## Examples

```bash
terrakube federated-claim delete --federated "$FED_ID" --id "$CLAIM_ID"
```
