# `terrakube federated-claim get`

Gets federated claim details.

## Usage

```bash
terrakube federated-claim get --federated <FED> --id <CLAIM_ID>
```

## Examples

```bash
terrakube federated-claim get --federated "$FED_ID" --id "$CLAIM_ID" --output json
```
