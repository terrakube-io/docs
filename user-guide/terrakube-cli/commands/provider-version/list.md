# `terrakube provider-version list`

Lists versions registered for a provider.

## Usage

```bash
terrakube provider-version list --organization <ORG> --provider <PROV>
```

## Examples

```bash
terrakube provider-version list -o "$ORG_ID" --provider "$PROV_ID" --output table
```
