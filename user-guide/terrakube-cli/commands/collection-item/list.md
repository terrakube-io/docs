# `terrakube collection-item list`

Lists items contained in a collection.

## Usage

```bash
terrakube collection-item list --organization <ORG> --collection <COL>
```

## Examples

```bash
terrakube collection-item list -o "$ORG_ID" --collection "$COL_ID" --output table
```
