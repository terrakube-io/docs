# `terrakube ssh get`

Gets SSH key details.

## Usage

```bash
terrakube ssh get --organization <ORG> --id <SSH_ID>
```

## Examples

```bash
terrakube ssh get -o "$ORG_ID" --id "$SSH_ID" --output json
```
