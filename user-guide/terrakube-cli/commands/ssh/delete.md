# `terrakube ssh delete`

Deletes an SSH key entry.

## Usage

```bash
terrakube ssh delete --organization <ORG> --id <SSH_ID>
```

## Examples

```bash
terrakube ssh delete -o "$ORG_ID" --id "$SSH_ID"
```
