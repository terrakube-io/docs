# `terrakube agent delete`

Unregisters an agent from an organization.

## Usage

```bash
terrakube agent delete --organization <ORG> --id <AGT_ID>
```

## Examples

```bash
terrakube agent delete -o "$ORG_ID" --id "$AGT_ID"
```
