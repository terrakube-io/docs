# `terrakube project-access list`

Lists access rules configured for a project.

## Usage

```bash
terrakube project-access list --organization <ORG> --project <PRJ>
```

## Examples

```bash
terrakube project-access list -o "$ORG_ID" --project "$PRJ_ID" --output table
```
