# `terrakube module list`

Lists private registry modules in an organization.

## Usage

```bash
terrakube module list --organization <ORG>
```

## Examples

```bash
terrakube module list -o "$ORG_ID" --output table
```
