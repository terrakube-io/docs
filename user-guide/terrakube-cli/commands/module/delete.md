# `terrakube module delete`

Deletes a module from the private registry.

## Usage

```bash
terrakube module delete --organization <ORG> --id <MOD_ID>
```

## Examples

```bash
terrakube module delete -o "$ORG_ID" --id "$MOD_ID"
```
