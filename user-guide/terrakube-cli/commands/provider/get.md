# `terrakube provider get`

Retrieves provider attributes.

## Usage

```bash
terrakube provider get --organization <ORG> --id <PROV_ID>
```

## Examples

```bash
terrakube provider get -o "$ORG_ID" --id "$PROV_ID" --output json
```
