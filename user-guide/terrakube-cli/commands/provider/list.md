# `terrakube provider list`

Lists private registry providers.

## Usage

```bash
terrakube provider list --organization <ORG>
```

## Examples

```bash
terrakube provider list -o "$ORG_ID" --output table
```
