# `terrakube provider delete`

Deletes a provider from the private registry.

## Usage

```bash
terrakube provider delete --organization <ORG> --id <PROV_ID>
```

## Examples

```bash
terrakube provider delete -o "$ORG_ID" --id "$PROV_ID"
```
