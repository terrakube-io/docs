# `terrakube template get`

Gets template details.

## Usage

```bash
terrakube template get --organization <ORG> --id <TPL_ID>
```

## Examples

```bash
terrakube template get -o "$ORG_ID" --id "$TPL_ID" --output json
```
