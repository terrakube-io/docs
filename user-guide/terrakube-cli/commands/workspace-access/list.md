# `terrakube workspace-access list`

Lists access rules for a workspace.

## Usage

```bash
terrakube workspace-access list --organization <ORG> --workspace <WS>
```

## Examples

```bash
terrakube workspace-access list -o "$ORG_ID" -w "$WS_ID" --output table
```
